export enum SortOrderEnum {
  Ascend = "ascend",
  Descend = "descend",
}

export enum SortFieldEnum {
  Name = "name",
  LastModified = "lastModified",
}

export enum SortModeEnum {
  Top = "top",
  Combine = "combine",
}
